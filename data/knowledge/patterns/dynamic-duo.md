---
category: "pattern"
---

# Dynamic Duo: sales_est + cfi_flag*(-ret)

`rank(sales_estimate_count) + rank(anl4_cfi_flag * (-1 * returns))`

Proven dynamic component pair for Fitness > 2.0 blends.
Analyst coverage breadth + cash flow revision reversal.

WARNING: `cfi_flag*(-ret)` is the #1 correlation driver.
See `rules/flag-ret-correlation.md`.
